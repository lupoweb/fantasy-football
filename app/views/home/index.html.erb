
<div class="container py-5 mt-4 ps-5">

  <h1 class="text-center display-2 mb-3" style="color: white; margin-top: 50px;">Fantabaretto's Teams <span class="background-gradient"></span></h1>
  
  <div class="container pt-3">
    <div class="row">
      <%Team.all.map do |team|%>
        <div class="col-12 col-md-6 col-lg-3 mt-3 mt-md-0">
          <div class="card blog-card" style="margin-top: 15px">
            <div class="text-center">
              <h2 class="mt-4 h4">
                <span class="highlight-underline"><%=image_tag team.image_url, :style => "border-radius: 50%; max-height:50px" %> <%=team.name%> </span>
              </h2>
              <p class="gray-500 px-4"> <%=team.owner%> </p>
            </div>
            <div class="text-left fs-small px-4 mt-5">
              <ul class="ps-2 pb-3" style="list-style-type: none;">
                <%Player.where(team_id: team.id).map do |player|%>
                  <li class="mt-3"><%=image_tag  player.img_url, :style => "border-radius: 50%; max-height:36px"%> <%= player.name %></li>
                <%end%>
              </ul>
            </div>
          </div>
        </div>
      <%end%>
  </div>
</div>
